# App Hosting configuration for Flutter Web
runConfig:
  # Specify the Flutter version
  runtime: flutter

build:
  # Build command for Flutter web
  buildCommand: flutter build web --release
  
  # Output directory where build artifacts are located
  outputDirectory: build/web

env:
  # Flutter build environment variables
  - variable: FLUTTER_BUILD_WEB
    value: "true"
